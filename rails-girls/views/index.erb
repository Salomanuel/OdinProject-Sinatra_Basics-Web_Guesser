<p>vote now</p>
<form action="cast" method="post">
	<ul>
		<% Choices.each do |id, text| %>
			<li>
				<input type="radio" name = "vote" value = <%= id %> />
				<%= text %>
			</li>
		<% end %>
	</ul>
	<button type="submit">Vote already!</button>
</form>